#Set our umask
umask 022

# Load profiles from $HOME/.base-config/etc/profile.d
if test -d $HOME/.base-config/etc/profile.d/; then
	for profile in $HOME/.base-config/etc/profile.d/*.sh; do
		test -r "$profile" && . "$profile"
	done
	unset profile
fi
